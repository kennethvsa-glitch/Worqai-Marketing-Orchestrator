---
date: 2026-07-09
workspace: C:\Users\kenne\OneDrive\Documentos\manifest-claude-system\projects\worqai-marketing-intelligence
executor_model: sonnet
tasks: 5
rule: Execute in order. No improvisation. This plan records the fixes completed
      in the 2026-07-09 pass.
---

## T1 - ADD - from F1

**Files**:
- `src/worqai_marketing_intel/prompt_runtime.py`
- `src/worqai_marketing_intel/cli.py`
- `src/worqai_marketing_intel/__init__.py`

**Action**: Add prompt runtime dispatch. Route audit/rewrite/review script
prompts to script audit, research/example prompts to benchmark research,
action-plan/location prompts to action planning, and all other prompts to brief
generation. Expose it as `run_prompt` and CLI command `run`.

**Verify**:
`$env:PYTHONPATH='src'; python -m worqai_marketing_intel run "Research successful examples for a WorqAI carousel" --json`

**Rollback**:
Delete `prompt_runtime.py`, remove `run` from `cli.py`, and remove `run_prompt`
exports from `__init__.py`.

## T2 - ADD - from F2

**Files**:
- `brand/spanish-latam-voice.md`
- `src/worqai_marketing_intel/script_audit_engine.py`
- `src/worqai_marketing_intel/orchestrator.py`
- `src/worqai_marketing_intel/cli.py`

**Action**: Add Spanish/LatAm script audit. The engine must return score,
diagnosis, hook audit, issues, what to keep, what to cut, rewritten script,
visual beats, best line, and CTA.

**Verify**:
`$env:PYTHONPATH='src'; python -m worqai_marketing_intel audit-script "Audit Spanish reel script for WorqAI | Estamos en una era..." --json`

**Rollback**:
Delete `script_audit_engine.py`, remove its orchestrator/CLI calls, and remove
the Spanish voice document if no other code reads it.

## T3 - REWRITE - from F3

**Files**:
- `src/worqai_marketing_intel/workspace_inspector.py`
- `src/worqai_marketing_intel/action_planner.py`
- `src/worqai_marketing_intel/verification_runner.py`

**Action**: Replace non-executable verification placeholders with executable
commands. For Python workspaces, emit `python -m compileall -q src` when `src`
exists and `python -m compileall -q .` otherwise. Let the planner prefer
compileall, pytest, lint, test, and build commands.

**Verify**:
`rg -n "python project detected" src tests`

**Rollback**:
Restore the previous inspector/planner/verifier behavior from Git.

## T4 - REWRITE - from F4

**Files**:
- `src/worqai_marketing_intel/verification_runner.py`

**Action**: Remove shell execution from verification. Parse only supported
commands into argv tuples: `npm run <script>`, `python -m compileall -q <path>`,
`py -m compileall -q <path>`, `python -m pytest`, `py -m pytest`, and `make`.
Skip unsupported commands with a clear result.

**Verify**:
`rg -n "shell=True" src`

**Rollback**:
Restore the previous verifier from Git.

## T5 - ADD - from F5

**Files**:
- `README.md`
- `AGENTS.md`
- `tests/test_orchestrator.py`

**Action**: Update docs to describe prompt runtime, script audit, branch-aware
execute, and current SQLite memory. Add tests for script audit, prompt dispatch,
Python verification command detection, dry-run execution, and unsupported
verification skipping.

**Verify**:
`python -m compileall -q src tests`

**Rollback**:
Restore README, AGENTS, and tests from Git.
